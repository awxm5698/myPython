from Src import my_image

if __name__ == '__main__':
    m = my_image.MyImage()
    # m.take_quadrilateral_img('Home', (100, 100), (400, 200))
    # m.take_square_img('Home', (100, 100), 200)
    # m.take_circular_img('Home', (200, 200), 100)
    # m.take_triangular_img('Home', (200, 200), (100, 400), (300, 400))
    # x, y, r = (100, 100, 100)
    # i = 1
    # for i in range(1, 5):
    #     m.take_circular_img('Home', (x*i, y*i), r, 'circular_{}'.format(i))

